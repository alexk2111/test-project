import { StatesFilterPipe } from './states-filter.pipe';

describe('StatesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StatesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
