import { StatusesFilterPipe } from './statuses-filter.pipe';

describe('StatusesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
